[project]
name = "fpl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Janarth Duraisingham", email = "jduraisingham108@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "ipykernel>=7.3.0",
    "jupyter>=1.1.1",
    "matplotlib>=3.11.1",
    "pandas>=3.0.5",
    "requests>=2.34.2",
]

[project.scripts]
fpl = "fpl:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
